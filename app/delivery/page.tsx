"use client";import {useEffect,useState} from "react";
export default function Delivery(){const [companies,setCompanies]=useState<any[]>([]),[name,setName]=useState(""),[loading,setLoading]=useState(true);async function load(){const r=await fetch("/api/delivery-companies",{cache:"no-store"}),x=await r.json();setCompanies(x.companies||[]);setLoading(false)}useEffect(()=>{load()},[]);async function add(n?:string,code?:string){const carrier=(n||name).trim();if(!carrier)return;const r=await fetch("/api/delivery-companies",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:carrier,code})}),x=await r.json();if(!r.ok)return alert(x.error||"Ajout impossible");setName("");load()}return <main className="dash-shell"><aside className="dash-side"><div className="dash-brand"><div className="brand-mark">M</div><div><b>Landing Motor</b><small>AI COD BUILDER</small></div></div><nav className="dash-nav"><a href="/">⌂ Dashboard</a><a href="/pages">▣ Landing Pages</a><a href="/orders">◎ Commandes</a><a href="/delivery" className="active">🚚 Livraison</a><a href="/products">◇ Produits</a><a href="/domains">⌁ Domaines</a><a href="/analytics">↗ Analytics</a><a href="/settings">⚙ Paramètres</a></nav></aside><section className="dash-content"><header className="dash-header"><div><span className="eyebrow">LANDING PAGE MOTOR</span><h1>Livraison COD</h1><p>Configure les transporteurs utilisés pour expédier tes commandes.</p></div></header><div className="delivery-setup"><section className="delivery-feature"><div className="delivery-logo">OZ</div><div><h2>Ozon Express Maroc</h2><p>Prépare Ozon Express comme transporteur principal. La connexion API sera activée avec tes identifiants Ozon.</p></div><button onClick={()=>add("Ozon Express","OZON_EXPRESS")}>+ Ajouter Ozon</button></section><section className="delivery-add"><h3>Ajouter un autre transporteur</h3><div><input value={name} onChange={e=>setName(e.target.value)} placeholder="Ex: Sendit, Cathedis, Ameex..."/><button onClick={()=>add()}>Ajouter</button></div></section><section className="delivery-list"><h3>Transporteurs actifs</h3>{loading?<p>Chargement...</p>:companies.length?companies.map(c=><article key={c.id}><span>🚚</span><div><b>{c.name}</b><small>{c.code}</small></div><em>Actif</em></article>):<div className="orders-empty">Aucun transporteur configuré.</div>}</section></div></section></main>}