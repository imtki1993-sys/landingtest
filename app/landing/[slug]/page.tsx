"use client"; import {useState} from "react";
export default function Landing({params}:{params:{slug:string}}){const [sent,setSent]=useState(false),[error,setError]=useState("");async function submit(e:any){e.preventDefault();setError("");const f=new FormData(e.currentTarget);const r=await fetch("/api/orders",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({slug:params.slug,name:f.get("name"),phone:f.get("phone"),city:f.get("city"),quantity:1})});if(r.ok)setSent(true);else setError("تعذر تسجيل الطلب. حاول مرة أخرى.")}return <div style={{maxWidth:620,margin:"auto",padding:24,textAlign:"center"}} dir="rtl"><h1>عرض خاص</h1><h2>{params.slug.replaceAll("-"," ")}</h2><p>التوصيل مجاني والدفع عند الاستلام</p>{sent?<h3>✅ تم تسجيل طلبك بنجاح</h3>:<form onSubmit={submit}><input name="name" required minLength={2} placeholder="الاسم الكامل"/><input name="phone" required inputMode="tel" minLength={8} placeholder="رقم الهاتف"/><input name="city" required placeholder="المدينة"/>{error&&<p>{error}</p>}<button className="primary">تأكيد الطلب</button></form>}</div>}