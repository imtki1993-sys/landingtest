"use client";
import {useState} from "react";
export default function Landing({params}:{params:{slug:string}}){const [sent,setSent]=useState(false);async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);await fetch("/api/orders",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({product:params.slug,name:f.get("name"),phone:f.get("phone"),city:f.get("city")})});setSent(true)}return <div style={{maxWidth:620,margin:"auto",padding:24,textAlign:"center"}}><h1>عرض خاص</h1><h2>{params.slug.replaceAll("-"," ")}</h2><p>التوصيل مجاني والدفع عند الاستلام</p>{sent?<h3>✅ تم تسجيل طلبك بنجاح</h3>:<form onSubmit={submit}><input name="name" required placeholder="الاسم الكامل"/><input name="phone" required inputMode="tel" placeholder="رقم الهاتف"/><input name="city" required placeholder="المدينة"/><button className="primary">تأكيد الطلب</button></form>}</div>}