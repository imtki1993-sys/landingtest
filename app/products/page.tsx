"use client";import {useEffect,useState} from "react";
export default function Products(){const [products,setProducts]=useState<any[]>([]);async function load(){const r=await fetch("/api/products"),x=await r.json();setProducts(x.products||[])}useEffect(()=>{load()},[]);return <main className="dash-shell"><aside className="dash-side"><div className="dash-brand"><div className="brand-mark">M</div><div><b>Landing Motor</b><small>AI COD BUILDER</small></div></div><nav className="dash-nav"><a href="/">⌂ Dashboard</a><a href="/pages">▣ Landing Pages</a><a href="/orders">◎ Commandes</a><a className="active" href="/products">◇ Produits</a><a href="/">⌁ Domaines</a><a href="/">↗ Analytics</a></nav><div className="dash-side-bottom"><a href="/">⚙ Settings</a></div></aside><section className="dash-content pages-dashboard"><header className="dash-header"><div><span className="eyebrow">CATALOGUE</span><h1>Mes produits</h1><p>Retrouve les produits utilisés dans tes landing pages et leurs performances COD.</p></div><a href="/"><button className="new-page-btn">+ Nouveau produit</button></a></header><div className="dash-stats"><div><small>PRODUITS</small><b>{products.length}</b><span>Catalogue actif</span></div><div><small>LANDING PAGES</small><b>{products.reduce((n,p)=>n+p.pages,0)}</b><span>Pages associées</span></div><div><small>COMMANDES</small><b>{products.reduce((n,p)=>n+p.orders,0)}</b><span>Total catalogue</span></div></div><div className="pages-panel"><div className="pages-toolbar"><div><b>Catalogue produits</b><span>{products.length} produit(s)</span></div><span className="pages-filter">Actifs</span></div><div className="pages-list">{products.map(p=><div className="page-row" key={p.id}><div className="page-thumb">{p.image?<img src={p.image} alt=""/>:<span>PR</span>}</div><div className="page-info"><b>{p.name}</b><small>{p.price} {p.currency}{p.compare_at_price?" · ancien "+p.compare_at_price:""}</small><span className={"status "+(p.is_active?"published":"draft")}>{p.is_active?"ACTIF":"INACTIF"}</span></div><div className="product-metrics"><span><b>{p.pages}</b> pages</span><span><b>{p.orders}</b> commandes</span></div><div className="page-actions"><a href={"/?product="+p.id}>Créer landing</a><a href="/pages">Pages</a></div></div>)}</div>{!products.length&&<p>Aucun produit.</p>}</div></section></main>}