"use client"; import {useState} from "react";
export default function Home(){const [form,setForm]=useState({name:"",price:"149",oldPrice:"",sourceUrl:"",description:"",language:"Darija Maroc"}),[page,setPage]=useState<any>(null),[loading,setLoading]=useState(false),[error,setError]=useState("");function set(k:string,v:string){setForm({...form,[k]:v})}async function generate(){setLoading(true);setError("");const r=await fetch("/api/generate",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(form)});const x=await r.json();if(r.ok)setPage(x.page);else setError(x.error||"Erreur");setLoading(false)}return <main><aside><h1>Landing Page Motor</h1><p>AI COD Builder</p><nav>Dashboard<br/>Landing Pages<br/><a href="/orders">Commandes</a><br/>Produits<br/>Domaines<br/>Analytics<br/>Settings</nav></aside><section><header><div><small>LANDING PAGE MOTOR</small><h2>Crée et publie tes landing pages COD</h2></div></header><div className="grid"><div className="card"><h3>Créer une landing page</h3><label>Nom du produit</label><input value={form.name} onChange={e=>set("name",e.target.value)} placeholder="Support téléphone moto"/><label>Prix (DH)</label><input value={form.price} onChange={e=>set("price",e.target.value)}/><label>Ancien prix (DH)</label><input value={form.oldPrice} onChange={e=>set("oldPrice",e.target.value)} placeholder="199"/><label>Lien Alibaba / AliExpress</label><input value={form.sourceUrl} onChange={e=>set("sourceUrl",e.target.value)} placeholder="https://..."/><label>Description</label><textarea value={form.description} onChange={e=>set("description",e.target.value)} placeholder="Décris ton produit..."/><label>Langue</label><select value={form.language} onChange={e=>set("language",e.target.value)}><option>Darija Maroc</option><option>Français</option><option>Arabe</option><option>English</option></select>{error&&<p>{error}</p>}<button className="primary" disabled={loading} onClick={generate}>{loading?"Création...":"✨ Créer la landing page"}</button></div><div className="card preview"><span>PREVIEW</span>{page?<><div className="mock"><b>{page.headline}</b><h2>{page.price} DH</h2>{page.oldPrice&&<p><s>{page.oldPrice} DH</s></p>}<p>{page.delivery}</p><button>{page.cta}</button></div><a href={page.url} target="_blank"><button className="publish">🚀 Ouvrir la landing page</button></a><p><small>Slug: {page.slug}</small></p></>:<p>Remplis les informations puis crée la landing page.</p>}</div></div></section></main>}