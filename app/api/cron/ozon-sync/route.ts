import {NextResponse} from "next/server";import {serviceDb,syncOzonOrder} from "../../../../lib/ozon";
export const maxDuration=60;
export async function GET(req:Request){try{const secret=process.env.CRON_SECRET;if(!secret)return NextResponse.json({error:"CRON_SECRET manquant"},{status:503});if(req.headers.get("authorization")!=="Bearer "+secret)return NextResponse.json({error:"Non autorisé"},{status:401});const s=serviceDb();const {data:carriers,error:ce}=await s.from("delivery_companies").select("id,workspace_id,settings").eq("code","OZON_EXPRESS").eq("is_active",true);if(ce)throw ce;let checked=0,updated=0,failed=0;for(const carrier of carriers||[]){const cfg:any=carrier.settings||{};if(!cfg.client_id||!cfg.api_key)continue;const {data:orders}=await s.from("orders").select("id,workspace_id,tracking_number,delivery_company_id,shipment_status,shipped_at,delivered_at,returned_at").eq("workspace_id",carrier.workspace_id).eq("delivery_company_id",carrier.id).not("tracking_number","is",null).not("shipment_status","in",'("DELIVERED","RETURNED","CANCELLED")').limit(100);for(const o of orders||[]){checked++;try{const before=o.shipment_status,x=await syncOzonOrder(s,o,cfg);if(x.shipment!==before)updated++}catch{failed++}}}return NextResponse.json({ok:true,checked,updated,failed,at:new Date().toISOString()})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}