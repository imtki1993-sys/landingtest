import {NextResponse} from "next/server";import {createClient} from "@supabase/supabase-js";
function db(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SECRET_KEY;if(!u||!k)throw new Error("Supabase env missing");return createClient(u,k,{auth:{persistSession:false}})}
export async function GET(){try{const s=db();const {data:products,error}=await s.from("products").select("id,name,slug,price,compare_at_price,cost_price,currency,is_active,description,created_at").is("archived_at",null).order("created_at",{ascending:false});if(error)throw error;const rows=await Promise.all((products||[]).map(async(p:any)=>{const [{count:pages},{count:orders},{data:lp}]=await Promise.all([s.from("landing_pages").select("id",{count:"exact",head:true}).eq("product_id",p.id),s.from("orders").select("id",{count:"exact",head:true}).eq("product_id",p.id),s.from("landing_pages").select("seo").eq("product_id",p.id).order("created_at",{ascending:false}).limit(1).maybeSingle()]);return {...p,pages:pages||0,orders:orders||0,image:(lp?.seo as any)?.images?.[0]||null}}));return NextResponse.json({products:rows})}catch(e:any){return NextResponse.json({error:e.message,products:[]},{status:500})}}