import {NextResponse} from "next/server";import {createClient} from "@supabase/supabase-js";
function db(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SECRET_KEY;if(!u||!k)throw new Error("Supabase env missing");return createClient(u,k,{auth:{persistSession:false}})}
export async function GET(){try{const s=db();const {data,error}=await s.from("domains").select("id,landing_page_id,hostname,type,is_primary,verification_status,ssl_status,last_error,last_checked_at,created_at,landing_pages(id,name,slug,status)").order("created_at",{ascending:false});if(error)throw error;return NextResponse.json({domains:data||[]})}catch(e:any){return NextResponse.json({error:e.message,domains:[]},{status:500})}}
export async function POST(req:Request){try{const b=await req.json(),hostname=String(b.hostname||"").trim().toLowerCase().replace(/^https?:\/\//,"").replace(/\/$/,"");if(!hostname||!b.landing_page_id)return NextResponse.json({error:"Domaine et landing page requis"},{status:400});const s=db();const {data:w}=await s.from("workspaces").select("id").limit(1).single();if(!w)throw new Error("Workspace introuvable");const {data,error}=await s.from("domains").insert({workspace_id:w.id,landing_page_id:b.landing_page_id,hostname,type:hostname.split(".").length>2?"SUBDOMAIN":"CUSTOM",verification_status:"PENDING",ssl_status:"PENDING"}).select().single();if(error)throw error;return NextResponse.json({domain:data},{status:201})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}