import {NextResponse} from "next/server";import {authContext} from "../../../../../lib/server-auth";
export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params,{s,workspaceId}=await authContext(req);const {data,error}=await s.from("order_status_history").select("id,source,status,note,created_at").eq("workspace_id",workspaceId).eq("order_id",id).order("created_at",{ascending:true}).limit(100);if(error)throw error;return NextResponse.json({history:data||[]})}catch(e:any){return NextResponse.json({error:e.message,history:[]},{status:500})}}