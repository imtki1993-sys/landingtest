import {NextResponse} from "next/server"; import {createClient} from "@supabase/supabase-js";
function db(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SECRET_KEY;if(!u||!k)throw new Error("Supabase env missing");return createClient(u,k,{auth:{persistSession:false}})}
function slugify(s:string){return s.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,70)}
export async function POST(req:Request){try{const p=await req.json();if(!p.name||!p.price)return NextResponse.json({error:"Nom et prix requis"},{status:400});const slug=slugify(p.name)+"-"+Date.now().toString().slice(-5);const lang=p.language==="Français"?"fr-MA":p.language==="English"?"en":"ar-MA";const {data,error}=await db().rpc("create_landing_product",{p_name:p.name,p_slug:slug,p_price:Number(p.price),p_old_price:p.oldPrice?Number(p.oldPrice):null,p_description:p.description||null,p_language:lang});if(error)throw error;return NextResponse.json({page:{headline:p.name,price:p.price,oldPrice:p.oldPrice||"",description:p.description||"منتج عملي مع الدفع عند الاستلام.",cta:"اطلب الآن",delivery:"التوصيل مجاني والدفع عند الاستلام",slug:data.slug,url:"/landing/"+data.slug},record:data})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}