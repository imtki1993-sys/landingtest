import {NextResponse} from "next/server";import {authContext} from "../../../lib/server-auth";
export async function POST(req:Request){try{const form=await req.formData(),files=form.getAll("images") as File[];if(!files.length)return NextResponse.json({urls:[]});if(files.length>10)return NextResponse.json({error:"Maximum 10 images"},{status:400});const {s,workspaceId}=await authContext(req),urls:string[]=[];for(const f of files){if(!f.type.startsWith("image/")||f.size>10*1024*1024)continue;const ext=(f.name.split(".").pop()||"jpg").toLowerCase(),path=`${workspaceId}/${Date.now()}-${crypto.randomUUID()}.${ext}`;const {error}=await s.storage.from("landing-images").upload(path,Buffer.from(await f.arrayBuffer()),{contentType:f.type,upsert:false});if(error)throw error;urls.push(s.storage.from("landing-images").getPublicUrl(path).data.publicUrl)}return NextResponse.json({urls})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}