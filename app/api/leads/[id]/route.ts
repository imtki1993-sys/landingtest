import {NextResponse} from "next/server";import {authContext} from "../../../../lib/server-auth";
const leadStatuses=["NEW","CONTACTED","CONFIRMED","NO_ANSWER","CALL_BACK","CANCELLED","SHIPPED","DELIVERED","RETURNED"];
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params,{s,workspaceId}=await authContext(req),b=await req.json(),patch:any={};if(b.status!==undefined){if(!leadStatuses.includes(b.status))return NextResponse.json({error:"Statut lead invalide"},{status:400});patch.status=b.status;if(b.status==="CONTACTED")patch.contacted_at=new Date().toISOString();if(b.status==="CONFIRMED")patch.confirmed_at=new Date().toISOString()}if(b.notes!==undefined)patch.notes=String(b.notes||"").slice(0,4000);if(b.full_name!==undefined){const name=String(b.full_name||"").trim();if(name.length<2)return NextResponse.json({error:"Nom invalide"},{status:400});patch.full_name=name.slice(0,160)}if(b.address!==undefined)patch.address=String(b.address||"").trim().slice(0,500)||null;if(b.city_name!==undefined)patch.city_name=String(b.city_name||"").trim().slice(0,120)||null;if(b.phone!==undefined){let phone=String(b.phone||"").replace(/[^0-9+]/g,"");if(phone.startsWith("0"))phone="+212"+phone.slice(1);else if(phone.startsWith("212"))phone="+"+phone;if(!/^\+212[5-7][0-9]{8}$/.test(phone))return NextResponse.json({error:"Numéro marocain invalide"},{status:400});patch.phone_raw=String(b.phone).trim();patch.phone_e164=phone}const {data,error}=await s.from("leads").update(patch).eq("id",id).eq("workspace_id",workspaceId).select("id,full_name,phone_raw,phone_e164,city_name,address,status,notes,contacted_at,confirmed_at").maybeSingle();if(error)throw error;if(!data)return NextResponse.json({error:"Lead introuvable"},{status:404});return NextResponse.json(data)}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}
