"use client";
import {FormEvent,useEffect,useState} from "react";
import {createClient} from "@supabase/supabase-js";
export default function ResetPassword(){
 const[p,setP]=useState("");const[msg,setMsg]=useState("");const[ready,setReady]=useState(false);
 useEffect(()=>{const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);s.auth.getSession().then(({data})=>setReady(!!data.session));const {data}=s.auth.onAuthStateChange((_event,session)=>setReady(!!session));return()=>data.subscription.unsubscribe()},[]);
 async function submit(e:FormEvent){e.preventDefault();if(p.length<8){setMsg("Le mot de passe doit contenir au moins 8 caractères.");return}const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);const {error}=await s.auth.updateUser({password:p});setMsg(error?error.message:"Mot de passe modifié. Tu peux maintenant te connecter.")}
 return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",background:"#070b14",padding:24,fontFamily:"Arial,sans-serif"}}><form onSubmit={submit} style={{width:"100%",maxWidth:420,background:"#101827",border:"1px solid #243044",borderRadius:20,padding:32,color:"white"}}><div style={{width:48,height:48,borderRadius:14,display:"grid",placeItems:"center",background:"#ff6b00",fontWeight:900,fontSize:24,marginBottom:22}}>M</div><h1>Nouveau mot de passe</h1><p style={{color:"#94a3b8"}}>{ready?"Choisis ton nouveau mot de passe.":"Ouvre cette page depuis le lien reçu par email."}</p><input type="password" minLength={8} required value={p} onChange={e=>setP(e.target.value)} placeholder="Nouveau mot de passe" style={{width:"100%",boxSizing:"border-box",padding:14,borderRadius:10,margin:"8px 0 18px",background:"#0b1220",color:"white",border:"1px solid #334155"}}/><button disabled={!ready} style={{width:"100%",padding:14,border:0,borderRadius:10,background:"#ff6b00",color:"white",fontWeight:800}}>Modifier le mot de passe</button>{msg&&<p>{msg}</p>}<p style={{textAlign:"center"}}><a href="/login" style={{color:"#ff8a3d"}}>Se connecter</a></p></form></main>
}