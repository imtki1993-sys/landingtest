"use client";import {useEffect,useState} from "react";
export default function Pages(){const [pages,setPages]=useState<any[]>([]),[busy,setBusy]=useState("");async function load(){const r=await fetch("/api/pages"),x=await r.json();setPages(x.pages||[])}useEffect(()=>{load()},[]);async function toggle(p:any){setBusy(p.id);await fetch("/api/pages/"+p.id,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status:p.status==="PUBLISHED"?"DRAFT":"PUBLISHED"})});await load();setBusy("")}
return <main><aside><h1>Landing Page Motor</h1><p>AI COD Builder</p><nav><a href="/">Dashboard</a><br/><b>Landing Pages</b><br/><a href="/orders">Commandes</a><br/>Produits<br/>Domaines<br/>Analytics<br/>Settings</nav></aside><section><header><div><small>LANDING PAGE MOTOR</small><h2>Mes landing pages</h2></div><a href="/"><button className="primary">+ Nouvelle page</button></a></header><div className="card"><div className="pages-list">{pages.map(p=><div className="page-row" key={p.id}><div className="page-thumb">{p.seo?.images?.[0]?<img src={p.seo.images[0]} alt=""/>:<span>LP</span>}</div><div className="page-info"><b>{p.name}</b><small>/{p.slug}</small><span className={"status "+p.status.toLowerCase()}>{p.status}</span></div><div className="page-actions"><a href={"/landing/"+p.slug} target="_blank">Voir</a><button disabled={busy===p.id} onClick={()=>toggle(p)}>{busy===p.id?"...":p.status==="PUBLISHED"?"Mettre en brouillon":"Publier"}</button></div></div>)}</div>{!pages.length&&<p>Aucune landing page.</p>}</div></section></main>}