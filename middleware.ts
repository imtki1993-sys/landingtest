import {NextRequest,NextResponse} from "next/server";
export async function middleware(req:NextRequest){const host=(req.headers.get("host")||"").split(":")[0].toLowerCase(),path=req.nextUrl.pathname;if(path.startsWith("/landing/")||path.startsWith("/api/")||path.startsWith("/_next/")||path.includes("."))return NextResponse.next();const own=[process.env.VERCEL_PROJECT_PRODUCTION_URL,"landingtest-kappa-ashy.vercel.app","landingtest-med-5dc0.vercel.app","localhost"].filter(Boolean).map(x=>String(x).replace(/^https?:\/\//,""));if(own.includes(host)||host.endsWith(".vercel.app"))return NextResponse.next();try{const r=await fetch(req.nextUrl.origin+"/api/domain-resolve?hostname="+encodeURIComponent(host),{headers:{"x-domain-resolve":"1"},cache:"no-store"}),x=await r.json();if(x.slug){const url=req.nextUrl.clone();url.pathname="/landing/"+x.slug;return NextResponse.rewrite(url)}}catch{}return NextResponse.next()}
export const config={matcher:["/((?!_next/static|_next/image|favicon.ico).*)"]};